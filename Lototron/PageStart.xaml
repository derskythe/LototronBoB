<Page x:Class="Lototron.PageStart"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="900" d:DesignWidth="1600"      
	  Title="PageStart" Background="{StaticResource FormStartBackground}" 
      Loaded="PageLoaded" Unloaded="PageUnloaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Button Grid.Row="2" Grid.ColumnSpan="2" VerticalAlignment="Bottom" Name="ButtonNext" HorizontalAlignment="Right" Style="{StaticResource StartLotteryButtonStyle}" Margin="50,50,50,50" Click="ButtonNextClick" />
    </Grid>
</Page>
